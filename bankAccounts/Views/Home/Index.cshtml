
@using bankAccounts.Models
@model LoginRegViewModel 

<!-- @{
    ViewData["Title"] = "Home Page";
}
<h1>Default Blank Page~</h1>
<p>Get to adding stuff</p> -->

<div asp-validation-summary="All"></div>

<h1>Login</h1>

<!-- asp-action ties form to the functon/route -->
<form asp-controller="Home" asp-action="login" method="post">
<span>
<!-- if model class is already imported up top, when setting asp, do not call on it again -->
<!-- asp-for only does validations, need name to take in the parameter -->


    <label asp-for="@Model.loginVM.email"></label>
    <input asp-for="@Model.loginVM.email"/>


    <label asp-for="@Model.loginVM.password"></label>
    <input asp-for="@Model.loginVM.password"/>


</span>
    <button type="submit">Login User</button>
</form>

<hr>


  



<h1>Registration</h1>

<!-- asp-controller = name of controller. FE HomeController is home -->
<!-- asp-action ties form to the functon/route -->
<form asp-controller="Home" asp-action="register" method="post">
<span>
<!-- if model class is already imported up top, when setting asp, do not call on it again -->
<!-- asp-for only does validations, need name to take in the parameter -->
    <label asp-for="@Model.registerVM.firstName"></label>
    <input  asp-for="@Model.registerVM.firstName"/>

    <label asp-for="@Model.registerVM.lastName"></label>
    <input  asp-for="@Model.registerVM.lastName"/>

    <label asp-for="@Model.registerVM.email"></label>
    <input asp-for="@Model.registerVM.email"/>


    <label asp-for="@Model.registerVM.password"></label>
    <input asp-for="@Model.registerVM.password"/>

   
    <label asp-for="@Model.registerVM.confirmRegPassword"></label>
    <input asp-for="@Model.registerVM.confirmRegPassword"/>
</span>
    <button type="submit">Create New User</button>
</form>

<hr>

        
            
              <!-- do not leave asp-for in forms if unused 
                 
    <form action="register" method="post" class="form-horizontal">
        <div>
            <label>Your First Name</label>
            <input  asp-for= "first_name"  type="text" class="form-control" >
        </div>
        <div>
            <label>Your Last Name</label>
            <input  asp-for="last_name" type="text" class="form-control" >
        </div>
        <div>
            <label>Your email</label>
            <input  asp-for="email" type="text" class="form-control" >
        </div>
        <div>
            <label>Password</label>
            <input asp-for="password" type="text" class="form-control" >
        </div>
        <div>
            <label>Confirm your Password</label>
            <input  name="password_confirm" type="text" class="form-control" >
        </div> -->
        
        <!-- name takes priority asp-for, type takes priority over what asp-for self assign is for. if type is empty  -->
        
        
    <!-- <button type="submit" class="btn btn-default">Send Registration</button>
    </form>  -->